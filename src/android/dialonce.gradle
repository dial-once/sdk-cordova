repositories {
    jcenter()

    // google() supported only in gradle 4.x
    // https://stackoverflow.com/a/46467800/902217
    maven { url 'https://maven.google.com' }
}

dependencies {
    compile ('com.dialonce:dialonce-android:2.6.16') {
    	exclude group: 'com.google.code.findbugs'
    }
}
